# Scaffolding generated by Casein v5.1.1.5

module Casein
  class BiOutsideSalesRepsController < Casein::CaseinController
  
    ## optional filters for defining usage according to Casein::AdminUser access_levels
    # before_filter :needs_admin, :except => [:action1, :action2]
    # before_filter :needs_admin_or_current_user, :only => [:action1, :action2]
  
    def index
      @casein_page_title = 'Bi outside sales reps'
  		@bi_outside_sales_reps = BiOutsideSalesRep.order(sort_order(:id)).paginate :page => params[:page]
    end
  
    def show
      @casein_page_title = 'View bi outside sales rep'
      @bi_outside_sales_rep = BiOutsideSalesRep.find params[:id]
    end
  
    def new
      @casein_page_title = 'Add a new bi outside sales rep'
    	@bi_outside_sales_rep = BiOutsideSalesRep.new
    end

    def create
      @bi_outside_sales_rep = BiOutsideSalesRep.new bi_outside_sales_rep_params
    
      if @bi_outside_sales_rep.save
        flash[:notice] = 'Bi outside sales rep created'
        redirect_to casein_bi_outside_sales_reps_path
      else
        flash.now[:warning] = 'There were problems when trying to create a new bi outside sales rep'
        render :action => :new
      end
    end
  
    def update
      @casein_page_title = 'Update bi outside sales rep'
      
      @bi_outside_sales_rep = BiOutsideSalesRep.find params[:id]
    
      if @bi_outside_sales_rep.update_attributes bi_outside_sales_rep_params
        flash[:notice] = 'Bi outside sales rep has been updated'
        redirect_to casein_bi_outside_sales_reps_path
      else
        flash.now[:warning] = 'There were problems when trying to update this bi outside sales rep'
        render :action => :show
      end
    end
 
    def destroy
      @bi_outside_sales_rep = BiOutsideSalesRep.find params[:id]

      @bi_outside_sales_rep.destroy
      flash[:notice] = 'Bi outside sales rep has been deleted'
      redirect_to casein_bi_outside_sales_reps_path
    end
  
    private
      
      def bi_outside_sales_rep_params
        params.require(:bi_outside_sales_rep).permit(:id, :code, :name)
      end

  end
end